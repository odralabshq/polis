# Copy to .env and fill in values.
# Required for: just build-vm

# Docker image version to export/bundle into the VM image.
# Defaults to "latest" (locally built images from `just build`).
# Set to a release tag (e.g. v0.3.0) to bundle specific GHCR images.
POLIS_IMAGE_VERSION=latest
