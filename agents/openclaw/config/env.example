# =============================================================================
# OpenClaw Environment Variables
# =============================================================================
# Copy this file to .env in project root and fill in your API keys.
#
# Quick start:
#   cp config/openclaw.env.example .env
#   # Edit .env and add at least one API key
#   ./cli/polis.sh init --profile=openclaw --local
#   # Open http://localhost:18789
# =============================================================================

# -----------------------------------------------------------------------------
# AI Provider API Keys (at least one required)
# -----------------------------------------------------------------------------
# OpenClaw auto-detects which key is available and sets the default model:
#
#   ANTHROPIC_API_KEY  -> anthropic/claude-sonnet-4-20250514
#   OPENAI_API_KEY     -> openai/gpt-4o
#   OPENROUTER_API_KEY -> openrouter/anthropic/claude-sonnet-4-20250514
#
# Priority: ANTHROPIC > OPENAI > OPENROUTER (first found is used)

# Anthropic Claude API key
# Get from: https://console.anthropic.com/
#ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key
# Get from: https://platform.openai.com/api-keys
#OPENAI_API_KEY=sk-proj-...

# OpenRouter API key (access multiple models)
# Get from: https://openrouter.ai/keys
#OPENROUTER_API_KEY=sk-or-...

# -----------------------------------------------------------------------------
# Gateway Configuration (optional)
# -----------------------------------------------------------------------------
# Gateway port (default: 18789)
#OPENCLAW_GATEWAY_PORT=18789

# Host port mapping (change if 18789 is already in use)
#OPENCLAW_HOST_PORT=18789

# Note: Gateway token is auto-generated inside the container on first start.
# Retrieve it with: ./cli/polis.sh openclaw token

# -----------------------------------------------------------------------------
# Optional: Web Search
# -----------------------------------------------------------------------------
# Brave Search API key for web search capability
# Get from: https://brave.com/search/api/
#BRAVE_SEARCH_API_KEY=

# -----------------------------------------------------------------------------
# Optional: Claude Web Session (for Claude.ai web interface)
# -----------------------------------------------------------------------------
# These are only needed if using Claude web interface instead of API
#CLAUDE_AI_SESSION_KEY=
#CLAUDE_WEB_SESSION_KEY=
#CLAUDE_WEB_COOKIE=
