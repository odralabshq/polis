# ClamAV base image (DHI local build)
# DHI clamav image runs as nonroot (UID 65532) with pre-owned directories
FROM dhi.io/clamav:1.5

# No modifications needed - configs mounted at runtime via docker-compose.yml
